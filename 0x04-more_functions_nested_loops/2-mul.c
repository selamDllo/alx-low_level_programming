#include<stdio.h>
/**
*	mul - print multile of two number
*	@a && @b: multiple both
*	Description: multiple two number and thier result
*	Return: always 0
*/
int mul(int a, int b)
{
	printf("%d\n", mul(a, b));
	return (0);
}
